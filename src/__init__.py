"""
src — Raiz do projeto FSE Entrega 2.

Domínios:
    common/         → infraestrutura compartilhada (HAL, config, exceptions, utils)
    traffic_system/ → Entrega 1: semáforos GPIO
    uart_protocols/ → Entrega 2: UART + Protocolo Simplificado + MODBUS RTU
"""
